target-version = "py311"
line-length = 120

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # long lines happen in prompt/system-prompt strings; not worth fighting

[lint.isort]
known-first-party = ["agent", "eval", "app"]
